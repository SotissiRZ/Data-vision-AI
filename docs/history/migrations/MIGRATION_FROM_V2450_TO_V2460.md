# Migration v2.45.0 → v2.46.0

Aucune migration de données n'est requise. Les turns assistant incluent désormais `agent_role`, `specialist_checks` et une `agent_trace` dans les métadonnées de réponse. Les clients anciens restent compatibles car ces champs sont additionnels.
