# Migration v2.48.0 → v2.49.0

No database migration is required.

Reports generated by older versions remain readable. New v2.49 reports use `block_schema_version: 1`, include a `content_hash`, `block_order`, and provenance metadata on every block. API callers may optionally provide `custom_blocks` and `block_order` when creating a report.
