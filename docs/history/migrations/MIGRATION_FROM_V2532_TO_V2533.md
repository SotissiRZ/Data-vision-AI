# Migration v2.53.2 → v2.53.3

Aucune migration de données. Cette version modifie la typographie frontend, le versioning et les gates de release.

Commande recommandée :

```powershell
docker compose down
docker compose up -d --build
```

Les volumes de données doivent être conservés.
